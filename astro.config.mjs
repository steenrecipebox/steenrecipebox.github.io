import { defineConfig } from 'astro/config';

export default defineConfig({
  // Change to your GitHub Pages URL or custom domain
  site: 'https://yourusername.github.io',
  // If repo is not <username>.github.io, add: base: '/repo-name'
});
